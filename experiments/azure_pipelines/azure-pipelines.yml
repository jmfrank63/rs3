jobs:
  - template: default.yml@templates
    parameters:
      minrust: FALSE

resources:
  repositories:
    - repository: templates
      type: github
      name: crate-ci/azure-pipelines
      ref: refs/heads/v0.4
      endpoint: rs3
